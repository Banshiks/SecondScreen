<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Полноэкранный видеодисплей -->
    <SurfaceView
        android:id="@+id/surfaceView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!-- Overlay статус (исчезает после подключения) -->
    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:padding="8dp"
        android:text="Не подключено"
        android:textColor="#AAFFFFFF"
        android:textSize="12sp"
        android:background="#44000000"/>

    <!-- Панель подключения (по центру) -->
    <LinearLayout
        android:id="@+id/layoutConnect"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="#CC1A1A1A">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="SecondScreen"
            android:textColor="#FFFFFF"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_gravity="center"
            android:layout_marginBottom="4dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="для Nothing Phone"
            android:textColor="#88FFFFFF"
            android:textSize="13sp"
            android:layout_gravity="center"
            android:layout_marginBottom="24dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="IP-адрес планшета"
            android:textColor="#AAFFFFFF"
            android:textSize="12sp"
            android:layout_marginBottom="6dp"/>

        <EditText
            android:id="@+id/etIpAddress"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:hint="192.168.1.100"
            android:inputType="textNoSuggestions"
            android:textColor="#FFFFFF"
            android:textColorHint="#44FFFFFF"
            android:background="#22FFFFFF"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:layout_marginBottom="16dp"/>

        <Button
            android:id="@+id/btnConnect"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Подключиться"
            android:textSize="15sp"/>

    </LinearLayout>

    <!-- Кнопка отключения (плавает поверх видео) -->
    <Button
        android:id="@+id/btnDisconnect"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="top|start"
        android:layout_margin="8dp"
        android:text="✕ Отключить"
        android:textSize="12sp"
        android:visibility="gone"
        android:alpha="0.7"
        android:backgroundTint="#CC333333"
        android:textColor="#FFFFFF"/>

</FrameLayout>
